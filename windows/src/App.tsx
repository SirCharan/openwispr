// Scaffold shell. The real windows (home, settings, onboarding) arrive in W5 and W6,
// built against the design system rather than styled ad hoc here.
export function App() {
  return (
    <main>
      <h1>OpenWispr</h1>
      <p>Windows build scaffold. Dictation lands in W2 to W4.</p>
    </main>
  );
}
